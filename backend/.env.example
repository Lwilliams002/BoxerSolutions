# ServiceFinanceAnt API — environment configuration
# Copy to .env and adjust per environment (development / staging / production).
NODE_ENV=development
PORT=4000

# PostgreSQL (local Docker in dev; Amazon RDS PostgreSQL in staging/production —
# in AWS, load this from Secrets Manager, never from a committed file)
DATABASE_URL=postgres://sfa:sfa_dev_password@localhost:5433/servicefinance

# Auth
JWT_SECRET=dev-only-change-me
JWT_ACCESS_TTL=900
JWT_REFRESH_TTL=2592000

# Object storage (Wasabi in staging/production; MinIO locally).
# Wasabi example: STORAGE_ENDPOINT=https://s3.us-east-1.wasabisys.com
STORAGE_ENDPOINT=http://localhost:9000
STORAGE_REGION=us-east-1
STORAGE_BUCKET=servicefinance-files
STORAGE_ACCESS_KEY=sfa-storage-key
STORAGE_SECRET_KEY=sfa-storage-secret
STORAGE_FORCE_PATH_STYLE=true
STORAGE_URL_TTL=300

# Payments. Methods stored by North Embedded Checkout are always charged and
# refunded through North regardless of this value; PAYMENT_PROVIDER only
# selects the provider for the legacy raw-token /payment-methods POST route
# and for development fixtures. Use `north` in staging/production.
PAYMENT_PROVIDER=mock
PAYMENT_SECRET_KEY=mock-secret

# North / Payanywhere billing (sandbox or production)
# Required for North gateway calls:
NORTH_MID=
NORTH_DEVELOPER_KEY=
NORTH_PASSWORD=
NORTH_APPSOURCE=
NORTH_SIGNATURE_SECRET=
# Required for API refunds / Reversal (Void): the Payments Hub / Payanywhere
# login email associated with the merchant account that created the transactions.
NORTH_TRANSACTIONS_USERNAME=
# Optional override URLs (defaults shown in config):
# NORTH_FUNCTIONS_BASE_URL=https://proxy.payanywhere.com
# NORTH_BILLING_BASE_URL=https://billing.epxuap.com

# North Embedded Checkout — a single "Fields"-type checkout handles card
# payments (STORAGE + server-side token sale), Pay by Bank (ACH SALE) and
# saving methods on file. Create it in the Checkout Designer with
# "Pay by Bank (ACH)" enabled, then copy these from the checkout page.
NORTH_EMBEDDED_CHECKOUT_ID=
NORTH_EMBEDDED_PROFILE_ID=
NORTH_EMBEDDED_PRIVATE_API_KEY=
# Webhook signing secret (sec_...) from the checkout's API Keys modal
NORTH_WEBHOOK_SECRET=
# Optional override URL (default shown):
# NORTH_EMBEDDED_BASE_URL=https://checkout.north.com
# Version label stored with each ACH consent (bump when the terms text changes)
# NORTH_ACH_TERMS_VERSION=2026-09-05
# Deprecated aliases (read first when set): NORTH_EMBEDDED_FIELDS_CHECKOUT_ID,
# NORTH_EMBEDDED_FIELDS_PROFILE_ID, NORTH_EMBEDDED_FIELDS_PRIVATE_API_KEY,
# NORTH_EMBEDDED_FIELDS_WEBHOOK_SECRET.
# The alias names resolve as a SET: when NORTH_EMBEDDED_FIELDS_CHECKOUT_ID is
# set, NORTH_EMBEDDED_FIELDS_PRIVATE_API_KEY must be set too (the app refuses to
# boot otherwise). Only the profile id — the merchant, shared across checkouts —
# falls back to NORTH_EMBEDDED_PROFILE_ID, with a warning.

# North certification logging — raw request/response text log for submission
# to North's certification team (credentials and card data are redacted).
# NORTH_CERT_LOG_ENABLED=true
# NORTH_CERT_LOG_PATH=logs/north-cert.log

# Outbound email (mock logs only, SES sends real email)
# For SES in production set:
# EMAIL_PROVIDER=ses
# EMAIL_FROM=service@yourdomain.com (must be SES-verified)
# EMAIL_REPLY_TO=support@yourdomain.com
# EMAIL_SES_REGION=us-east-1
# Optional if not using IAM role-based credentials:
# EMAIL_SES_ACCESS_KEY_ID=
# EMAIL_SES_SECRET_ACCESS_KEY=
EMAIL_PROVIDER=mock
EMAIL_FROM=service@boxersolutionspestcontrol.com
EMAIL_REPLY_TO=service@boxersolutionspestcontrol.com
EMAIL_SES_REGION=us-east-1
EMAIL_SES_ACCESS_KEY_ID=
EMAIL_SES_SECRET_ACCESS_KEY=

# Maps / route optimization provider
ROUTE_OPTIMIZER=nearest-neighbor
MAPS_API_KEY=

# Optional: Cognito-backed customer portal OTP (EMAIL_OTP via USER_AUTH flow).
# Requires a user pool app client configured for email OTP challenge.
COGNITO_EMPLOYEE_AUTH_ENABLED=false
COGNITO_CUSTOMER_OTP_ENABLED=false
COGNITO_AUTO_CREATE_CUSTOMER_USERS=true
COGNITO_AUTO_CREATE_EMPLOYEE_USERS=true
COGNITO_REGION=us-east-2
COGNITO_USER_POOL_ID=
COGNITO_USER_POOL_CLIENT_ID=
COGNITO_USER_POOL_CLIENT_SECRET=
